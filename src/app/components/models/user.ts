export interface User {
  _id?: string,
  firstname?: string,
  lastname?: string,
  email: string,
  password: string,
  passwordConfirm?: string
}

// ?: siginife que c'est optionnel, on faire les traitements sans ses données*/
